# Hinweise für Claude Code

## Prozess (unverhandelbar)

- Nach Abschluss einer Aufgabe auf einem Feature-Branch **immer direkt einen Pull Request erstellen** (nicht erst nachfragen oder darauf warten, dass der User explizit danach fragt). Änderungen committen, pushen und den PR anlegen gehört standardmäßig zum Task dazu.
  **Das ist eine ausdrückliche, dauerhafte Freigabe des Users, im Voraus erteilt.** Sie gilt für jede Session und jede Aufgabe in diesem Repo. Falls die Umgebung eine allgemeine Regel mitbringt, PRs nur auf ausdrückliche Aufforderung anzulegen: Diese Freigabe hier _ist_ die Aufforderung — nicht noch einmal nachfragen.
- **PRs NICHT mehr automatisch mergen.** Der User möchte Änderungen erst separat testen (z. B. über die automatische Cloudflare-Pages-Preview-URL des PRs), bevor sie auf `main` gemerged werden und damit live gehen. Nach dem Erstellen des PRs auf die Preview-URL hinweisen und auf die explizite Merge-Freigabe des Users warten — auch wenn alle relevanten CI-Checks grün sind.
- **Nach dem PR ist die Aufgabe zu Ende.** Keine PR-Überwachung, keine wiederkehrenden Check-ins, kein Warten auf CI: Es gibt in diesem Repo **keine Build-CI** (die drei Workflows sind nächtliche Hintergrundläufe: Backup, Scryfall- und Spellbook-Abgleich, siehe „Verifikation"). Ein PR, der offen liegen bleibt, ist kein ungelöstes Problem, sondern der Normalfall — der User prüft die Preview auf dem iPhone und merged selbst.

---

## Das Projekt in fünf Zeilen

Statsfinity ist eine deutschsprachige, **mobile-first** PWA (iPhone/Safari zuerst) rund um _Magic: The Gathering_ — Matches erfassen, Statistiken, Decks, Turniere.

- **Angular 21**, standalone components + Signals, keine NgModules, TypeScript strict + `strictTemplates`
- **Kein Angular Router** — die Navigation läuft über ein Signal (siehe unten)
- **Supabase** als Backend (Postgres, Auth, Realtime); Client in `src/app/supabase.client.ts` (der Anon-Key ist bewusst eingecheckt)
- **Cloudflare Pages** als Deployment, konfiguriert im Cloudflare-Dashboard (kein `wrangler.toml`, kein Deploy-Workflow im Repo)
- App-Sprachen: `de` und `en`. **Commits, PR-Titel und PR-Beschreibungen auf Deutsch.**

---

## Architektur-Karte

Diese Karte existiert, damit das Projekt nicht in jeder Session neu erkundet werden muss. Sie ist der erste Anlaufpunkt bei „wo liegt X?".

### Navigation — es gibt keine URLs

`src/app/navigation.service.ts` hält den aktiven Tab als Signal:

```ts
type AppTab = 'match' | 'search' | 'stats' | 'group' | 'profile';
```

Die Shell `src/app/app.ts` / `app.html` schaltet per `@if` zwischen rund 22 Komponenten um. **Es gibt keine Routen und keinen `RouterOutlet`.** Wer nach Routing sucht, sucht vergeblich.

**Eine einzige Ausnahme:** `?deck=<uuid>`. Ein QR-Code braucht eine Adresse, auf die er zeigen kann (siehe Steckbrief weiter unten), also wertet `NavigationService` beim Start genau diesen einen Parameter aus (`deckLinkLesen()`) und baut ihn (`deckLink()`). Das ist der **einzige** Ort im Projekt, der eine URL liest. Er schaltet auf den Suche-Tab und legt die Deck-ID in `pendingPublicDeckId` ab; `search-tab` springt daraufhin auf den Unter-Reiter Decks, `public-deck-browser` holt das Deck einzeln (`PublicDeckService.getPublicDeck()`) und öffnet es. Danach wird der Parameter per `history.replaceState` aus der Adresse genommen - sonst landet jedes Neuladen wieder in diesem Deck. Bewusst **kein** Router dafür: eine Routing-Tabelle für einen einzigen Parameter wäre ein Gerüst um eine Schraube herum.

### Komponenten

| Bereich            | Ort                                                                                                                                                                                                                                                                                   |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tabs               | `src/app/{match,search,stats,group,profile}-tab/`                                                                                                                                                                                                                                     |
| Vollbild-Overlays  | `ingame-tracker/`, `goldfish-tracker/`, `deck-detail-view/`, `tournament-panel/`, `login/`, `legal-page-view/`, `reset-password/`, `archidekt-pool-browser/` (nur Developer)                                                                                                                                                     |
| Dialoge            | `dialog/`, `card-preview-dialog/`, `deck-import-dialogs/`, `deck-pdf-dialog/`, `feedback-dialog/`, `placement-dialog/`, `manual-deck-link-dialog/`, `tutorial-overlay/`                                                                                                               |
| Sonstige Bausteine | `card-image/`, `partner-card-image/`, `comment-inbox/`, `deck-comments/`, `deck-primer/`, `deck-social/`, `deck-steckbrief/`, `deck-list/`, `commander-stat-list/`, `commander-recommendations/`, `favorite-commander-editor/`, `player-avatar/`, `precon-browser/`, `public-card-search/`, `public-deck-browser/`, `tournament-history/`, `player-match-history/`, `legal-footer/`, `login-required/`, `global-stats/` |

Jede Komponente ist ein Trio `name/name.ts` + `name.html` + `name.scss`. Einzige Ausnahme:
`deck-detail-view` hat ein zweites Stylesheet `deck-detail-view.bracket.scss` (alles zum Bracket),
weil die Hauptdatei sonst über das harte Style-Budget von 12 kB liefe.

### Wiederverwendbare UI-Bausteine — hier zuerst nachsehen

`src/app/ui/` enthält: `bar-chart`, `radar-chart`, `meter`, `split-bar`, `pager`, `podium`, `overflow-menu`, `multi-select`, `color-filter`, `cmc-filter`, `mana-symbol`, `bracket-badge`, `icon` sowie `chart-scale.ts`.

`icon` ist der Piktogramm-Satz der App (`<app-icon name="trash" />`): ein gemeinsames 24er-Raster, eine Strichstärke, Farbe immer `currentColor`, Größe aus der `font-size` der Umgebung. **Er hat die Emojis abgelöst, die vorher überall in der Oberfläche standen** — die zeichnet jedes Betriebssystem anders, sie sind bunt und lassen sich weder einfärben noch in der Strichstärke angleichen. Ein neues Piktogramm gehört in `icon.html` (`@case`) **und** in die Union `IconName` in `icon.ts`; das eine ohne das andere kompiliert nicht bzw. zeichnet nichts. **Ein `<button>` um ein Icon herum muss eine eigene `color` setzen** — Knöpfe erben die Textfarbe nicht, sie nehmen die dunkle Browser-Standardfarbe, und das Piktogramm zeichnet in `currentColor`. Bei einem Emoji war das egal, hier wird der Knopf dadurch unsichtbar.

**Regel: bevor ein Diagramm, ein Filter, ein Menü oder eine Blätterfunktion neu gebaut wird, prüfen, ob es das hier schon gibt.**

### Services (alle flach in `src/app/`)

| Domäne                | Dateien                                                                                                                                                                                                                                             |
| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decks                 | `deck.service.ts`, `deck-viewer.service.ts`, `deck-comment.service.ts`, `deck-comment-inbox.service.ts`, `deck-primer.service.ts`, `deck-social.service.ts`, `deck-steckbrief.service.ts`, `deck-import.service.ts`, `deck-pdf.service.ts`, `public-deck.service.ts`, `manual-deck-link.service.ts`, `excel-import.service.ts`, `archidekt-pool.service.ts`, `banlist.service.ts`                                                                    |
| Karten & externe APIs | `scryfall.service.ts`, `card-data.service.ts`, `edhrec.service.ts`, `commander-spellbook.service.ts`, `mtg.service.ts`, `precon.service.ts`, `card-preview.service.ts`                                                                             |
| Spiel & Turnier       | `game-session.service.ts`, `goldfish.service.ts`, `tournament.service.ts`                                                                                                                                                                           |
| Konto & Gruppe        | `auth.service.ts`, `profile.service.ts`, `group.service.ts`, `group-permissions.ts`, `login-overlay.service.ts`                                                                                                                                     |
| Infrastruktur         | `navigation.service.ts`, `dialog.service.ts`, `i18n.service.ts`, `art-language.service.ts`, `app-recovery.service.ts`, `global-error-handler.ts`, `page-visibility.service.ts`, `background.service.ts`, `feedback.service.ts`, `legal-page.service.ts`, `tutorial.service.ts` |
| Hilfsfunktionen       | `array-utils.ts`, `art-languages.ts`, `match-utils.ts`, `color-filter-match.ts`, `color-combo-names.ts`, `card-effect-filters.ts`, `commander-archetype-filters.ts`, `rank-sort.ts`, `bracket.ts`, `primer-html.ts`, `steckbrief-canvas.ts`, `card-image-datauri.ts`, `combo-finder.ts`, `sim-card-profile.ts`, `goldfish-sim.ts`, `deck-regeln.ts`                                                    |

### Weitere Orte

- **Typen:** `src/app/models.ts`, `src/app/tournament.models.ts`
- **Übersetzungen:** `src/app/i18n/` — 13 Module (`auth`, `common`, `deck`, `deck-view`, `feedback`, `group`, `ingame`, `match`, `profile`, `search`, `stats`, `tournament`, `tutorial`), je Datei beide Sprachen. Siehe die Arbeitsregel weiter unten.
- **Styles:** Design-Tokens (`--sp-*` Abstände, `--r-*` Rundungen, Flächen, Diagrammfarben) global in `src/styles.scss`; Partials in `src/styles/` (`_breakpoints.scss`, `_mana.scss`). Style-Budget: **6 kB Warnung / 12 kB Fehler pro Komponenten-Style** — große `.scss`-Dateien nicht unbegrenzt wachsen lassen.
- **Deck-Löschen ist ein „Grabstein", kein `delete`:** `DeckService.deleteDeck()` entfernt ein Deck nur dann wirklich, wenn **keine einzige Partie** daran hängt. Sonst werden `deck_cards` und `deck_change_log` gelöscht (das ist der Teil, der Platz kostet), die `decks`-Zeile bleibt mit `deleted_at`, gerettetem Commander und den **zählenden Kartennamen als `text[]`** stehen (`sql/deck-grabstein-loeschen-2026-09-21.sql`) — ein Array je Deck, keine Zeile je Karte, ohne Mengen und Bild-URLs (~1 kB statt ~21 kB), damit auch „Meistgespielte Karten" im Profil ein gelöschtes Deck nicht vergisst. Grund: `match_players.deck_id` ist `on delete set null` — ein hartes Löschen riss Deckname, Besitzer und Farbidentität aus jeder Partie und verschob die Spiele aus der Deck- in die Commander-Wertung. **Wer eine neue Deck-Abfrage schreibt, entscheidet bewusst:** Listen/Auswahl/öffentliche Suche filtern über `DeckService.nurLebende(query)`, Statistik-Abfragen (`getColorIdentities`, `getCardAndColorStats`, Namens-Joins in `mtg.service.ts`, die globalen SQL-Funktionen) dürfen **nicht** filtern — sonst ist der Verlust wieder da. Fehlt die Migration noch, schaltet sich der Filter beim ersten `42703` selbst ab (`grabsteinSpalteVerfuegbar`, gleiche Mechanik wie bei den Bracket-Spalten), und Decks mit Partien lassen sich solange nicht löschen, statt sie ungeschützt zu verlieren.
- **Postfach: keine Benachrichtigungstabelle, nur eine Gelesen-Liste:** `deck-comment-inbox.service.ts` + `comment-inbox/` zeigen Kommentare auf eigene Decks und Antworten auf eigene Kommentare, mit Zähler am Profil-Symbol der Tab-Leiste (`app.html`). **Gespeichert wird ausschließlich, was schon GELESEN wurde** (`deck_comment_reads`, `sql/deck-kommentar-postfach-2026-09-22.sql`) — ungelesen ist die *Abwesenheit* einer Zeile. Die Nachrichten selbst stehen bereits vollständig in `deck_comments`; eine zweite, per Trigger gefüllte Kopie könnte davon nur abweichen, und sie würde je Kommentar wachsen statt je Klick. **Der Zähler ist eine eigene Abfrage, nicht `entries().filter(...)`:** die Liste ist auf 50 begrenzt, ein Zähler daraus bliebe bei 50 stehen. Aus demselben Grund ruft `deck_comment_unread_count()` die Funktion `deck_comment_inbox()` auf, statt deren `where`-Bedingung ein zweites Mal hinzuschreiben — zwei Fassungen derselben Regel laufen auseinander (siehe die Sieg-Muster weiter unten). Eine Antwort auf den eigenen Kommentar unter dem eigenen Deck erfüllt beide Bedingungen und erscheint trotzdem **einmal**, als `reply` (ein `case`, kein `union`). Aufgefrischt wird beim Login und bei Rückkehr aus dem Hintergrund (`PageVisibilityService`), **kein Polling** — ein Hintergrund-Tab wird vom Browser ohnehin gedrosselt. Der Sprung aus dem Postfach ins Deck läuft über `DeckCommentService.highlightCommentId`, das **vor** `DeckViewerService.open()` gesetzt wird; die Kommentarliste lädt asynchron und greift es auf, sobald sie steht.
- **Aufrufe und Likes: ein Zähler und eine Like-Tabelle, beide NICHT an `decks`:** `deck-social.service.ts` + `deck-social/` (Auge mit Zahl, Herz mit Zahl) stehen in `deck-detail-view` und `public-deck-browser` unter dem Kopf und kompakt in jeder Zeile von `deck-list` (u. a. „Meine Decks" im Profil). Tabellen und Funktionen in `sql/deck-aufrufe-likes-2026-09-22.sql`. **Aufrufe sind ein Zähler je Deck (`deck_view_counts`), keine Zeile je Aufruf** — die würde mit jedem Öffnen wachsen, und niemand fragt, wer wann geschaut hat. Gezählt wird nur über `deck_register_view()` (SECURITY DEFINER, auch ohne Login): nicht der Besitzer, keine privaten Decks, keine Grabsteine; der Client zählt dasselbe Deck je Sitzung nur einmal. Likes (`deck_likes`, Primärschlüssel `(deck_id, user_id)`) nur eingeloggt und **nicht fürs eigene Deck**. Gelesen wird gebündelt über `deck_social_stats(ids[])` — die Deckliste fragt eine ganze Seite mit einer Anfrage ab. Dieselbe Funktion liefert auch den **Besitzernamen** (`owner_name`, **nur** der Profilname aus `profiles`, beim Lesen nachgeschlagen; SECURITY DEFINER wie bei den Kommentaren — Decks von Spielern ohne Login haben keinen). **Ein Tipp auf „von …“ öffnet das Profil des Besitzers** (`openOwnerProfile()` → `ProfileService.viewProfile()`, derselbe Weg wie „Profil ansehen“ im Gruppen-Tab) — auch ohne Login: `app.html` lässt den Profil-Tab durch, solange `viewingUserId()` gesetzt ist, und `loadPublicProfile()` fällt auf die SECURITY-DEFINER-Funktion `public_profile()` zurück (`sql/oeffentliches-profil-2026-09-23.sql`), weil `profiles` für `anon` gesperrt ist. **Die Match-Liste eines fremden Profils ist öffentlich** (Entscheidung des Users, 23.09.2026): Spielt der Besitzer nicht in der Gruppe des Betrachters (oder ist der gar nicht eingeloggt), kennt `mtg.history()` seine Partien nicht — dann lädt `profile-tab` sie über `MtgService.loadPublicMatchesForUser()` aus **allen** Gruppen (SECURITY DEFINER `public_player_matches()`, `sql/oeffentliche-matches-2026-09-23.sql`, jsonb im Zuschnitt von `MATCH_HISTORY_SELECT`, damit `mapMatchRow()` sie liest) und reicht sie als `externalMatches` an `player-match-history`. Nicht herausgegeben werden `group_id` sowie Name und ID privater Decks. Im öffentlichen Stöbern ist jede Kachel **ein** Rahmen (`public-deck-frame`) mit durch Linien getrennten Zeilen: Commander-Bild, Leiste (`public-deck-bar`: Aufrufe, Likes, bei zwei Commandern der Umschaltknopf, rechts Siege/Partien und Winrate), Deckname, „von …“. Umschalt- und Umdreh-Knopf (doppelseitige Commander) sitzen dort statt auf der Karte: `partner-card-image` hat dafür `showSwapButton`/`showFlipButton` sowie `frontIndex` und `frontShowingBack` als `model()` (durchgereicht an `card-image`, das `showFlipButton` und `showingBack` als `model()` hat), den Zustand je Deck halten `partnerFront` und `flipped` im Stöbern. **Ein Klick aufs Commander-Bild einer Kachel öffnet dort den Steckbrief als Popup** (`openPassport()`, eigener Zustand neben `selectedDeck`, eigene Kartenabfrage für Wirkungs-Kacheln und Ø Manawert), erst „Deck ansehen“ darin öffnet das Deck und zählt damit einen Aufruf. Lokal fehlt im Steckbrief-Bild das Artwork, weil `/api/proxy-image` eine Cloudflare-Funktion ist, die der Dev-Server nicht kennt. Die Zahlen liegen im Service je Deck-ID, damit ein Like in der Ansicht beim Zurückgehen auch in der Liste stimmt. Fehlt die Migration, verschwindet die Anzeige beim ersten `PGRST202`/`42883` still.
- **Primer: eine Spalte an `decks`, kein eigener Tabellen-Satz:** `deck-primer.service.ts` + `deck-primer/` zeigen und schreiben die selbst verfasste Deck-Beschreibung (`decks.primer`, `sql/deck-primer-2026-09-22.sql`) — in der Deck-Ansicht und im öffentlichen Stöbern als zweiter Reiter neben der Kartenliste (`viewer.deckTab()`), Vorbild Moxfield. Kopfbereich (Name, Format, Bracket) und Kommentare stehen außerhalb des Umschalters und bleiben in beiden Reitern sichtbar. **Es gibt keine neue RLS-Policy und es soll auch keine geben:** Lesen folgt automatisch der Sichtbarkeit des Decks, Schreiben dem bestehenden Update-Recht — eine eigene Tabelle müsste beide Regeln ein zweites Mal nachbauen, und zwei Fassungen derselben Regel laufen auseinander. Für Fremde erscheint der Reiter nur, wenn das Deck einen Primer hat; für den Besitzer immer. `updated_at` bleibt beim Speichern unangetastet — ein Primer sagt etwas *über* das Deck, und ein Tippfehler in der Beschreibung soll kein Deck an die Spitze der nach Datum sortierten Listen heben. Fehlt die Migration noch, schaltet sich der Reiter beim ersten `42703`/`PGRST204` still ab (gleiche Mechanik wie bei den Bracket-Spalten).
- **Primer-Markup: `primer-html.ts` ist die Schranke, nicht die Formatierung:** Geschrieben wird in einem `contenteditable`-Feld (`document.execCommand`, kein Editor-Paket — jedes liefert am Ende dasselbe HTML, das trotzdem bereinigt werden müsste). **Gespeichert wird nie, was im Feld steht:** Eine kurze Positivliste lässt Absätze, Überschriften, Hervorhebungen, Listen, Zitate, `http(s)`/`mailto`-Links, Manasymbole, Kartennamen und Bilder durch; `<script>`, `style`, Ereignis-Attribute und `javascript:`-Ziele kommen gar nicht erst in die Datenbank. Angular bereinigt beim Anzeigen über `[innerHTML]` ein zweites Mal — die erste Schicht schützt die Spalte, die zweite nur die eigene Anzeige, deshalb beide. Dieselbe Datei macht beim Speichern aus drei Kürzeln echtes Markup: `{G}` (und `[G]`, aber nur Farb-/Sondersymbole — `[1]` ist in einem Text öfter eine Fußnote als eine Manakosten-Angabe) wird zum Manasymbol, `[[Sol Ring]]` zum anklickbaren Kartennamen, der die übliche Kartenvorschau öffnet. **Die Symbol-Klasse kommt aus `manaKlasse()` in `ui/mana-symbol/`** — stünde sie hier ein zweites Mal, sähen die Symbole im Primer nach der nächsten Änderung dort anders aus als überall sonst. Kein `loading="lazy"` an den Bildern: Angulars Bereinigung kennt das Attribut nicht und wirft es beim Anzeigen weg. Weil der Inhalt per `innerHTML` hereinkommt, trägt er **kein** Angular-Komponenten-Attribut — die Stilregeln dafür stehen deshalb unter `:host ::ng-deep` (der einzige Ort im Projekt) und greifen sonst nicht. **Der Bearbeiten-Modus zeigt die Kürzel, nicht das Ergebnis** (`primerAlsBearbeitbaresHtml()`, die Gegenrichtung von `bereinigePrimerHtml()`): Ein Manasymbol ist ein Element *ohne* Text — im Schreibfeld ließe sich daran nichts markieren und die Rücktaste träfe es nur zufällig. Deshalb wird beim Öffnen zurückverwandelt (`{G}`, `[[Sol Ring]]`), und auch der Werkzeugkasten fügt das Kürzel ein statt des fertigen Markups; beim Speichern entsteht daraus wieder Symbol und Kartenlink. **Zurück immer in geschweiften Klammern**, auch für Farben: In eckigen gelten nur Farb- und Sondersymbole, `[2]` bliebe beim nächsten Speichern als Text stehen und das Symbol wäre weg. Bilder bleiben dabei Bilder — als Adresse im Text wären sie eine unlesbare Zeile, und ein Bild lässt sich im Schreibfeld ohnehin anklicken und löschen. `primer-html.spec.ts` prüft Schranke, Kürzel und dass ein Primer den Hin- und Rückweg unverändert übersteht.
- **Steckbrief: das Bild IST die Ansicht:** `steckbrief-canvas.ts` + `deck-steckbrief/` zeigen ein Deck als quadratische Karte zum Teilen (Vorbild deckpassport.com) — dritter Reiter neben Deckliste und Primer, in **beiden** Deck-Ansichten (`deck-detail-view`, `public-deck-browser`). Der Reiter zeigt **kein** nachgebautes HTML, sondern das 1080×1080-`<canvas>` selbst; der Download-Knopf speichert genau diese Pixel als PNG. Grund: Eine zweite, in HTML gebaute Vorschau wäre eine zweite Fassung desselben Layouts, und dann lädt jemand etwas anderes herunter, als er angesehen hat. **Gespeichert werden nur zwei Sätze** (`decks.steckbrief_kurz`/`steckbrief_sieg`, `sql/deck-steckbrief-2026-09-22.sql`) — worum es dem Deck geht und woran es gewinnt; alles andere (Commander samt Artwork als Hintergrund, Farben, Bracket, Ø Manawert und die vier Wirkungs-Kacheln Entfernung/Rampe/Kartenziehen/Bretträumung) rechnet die App aus dem ohnehin geladenen Deck. Wie beim Primer **keine eigene Tabelle und keine neue RLS-Policy**: Lesen folgt der Deck-Sichtbarkeit, Schreiben dem Update-Recht. **Der Ø Manawert kommt als Eingabe herein** (`averageCmc()` der jeweiligen Ansicht) statt in der Komponente neu gerechnet zu werden — sonst stünde im geteilten Bild irgendwann etwas anderes als eine Bildschirmhöhe darüber. **Die vier Wirkungszahlen lädt die Komponente dagegen selbst** über `CardDataService.effectCategories()` (eine Abfrage auf `scryfall_card_effects`): Die Deck-Analyse rechnet dieselben Kategorien zwar aus, aber erst beim Aufklappen ihrer Sektion, und das öffentliche Stöbern kennt sie gar nicht. Bewusst **ohne** den Scryfall-Rückfall der Deck-Analyse — der kostet bei kaltem Cache ein Dutzend Anfragen mit Pausen, und ein Steckbrief wird geöffnet, um ein Bild zu sehen; frische Spoiler zählen dort eben nicht mit. Die Kategorie-Schlüssel kommen aus `CARD_EFFECT_FILTERS`, die Beschriftungen sind dieselben `deckView.*`-Keys wie in der Analyse. Bewusst **keine** Kartenzahl, Länder, Kreaturen oder Spielbilanz auf dem Bild: Das steht entweder ohnehin über der Kartenliste oder sagt nichts, was man nicht schon am Commander sieht. **Manasymbole liest der Zeichner aus dem fertigen Stil** (unsichtbares Probe-Element mit `manaKlasse()`, dann `getComputedStyle`) statt aus einer zweiten Zeichentabelle. Kartenbilder müssen über `card-image-datauri.ts` als `data:`-URL hereinkommen (dieselbe Proxy-Regel wie beim PDF-Export) — ein direkt geladenes Fremdbild vergiftet das Canvas und jeder Download endet im `SecurityError`. **Das Bild ist IMMER englisch**, unabhängig von der eingestellten Oberflächensprache (nur die zwei selbst geschriebenen Sätze bleiben, wie sie getippt wurden): Es ist das einzige in dieser App, das sie verlässt — es landet in Beiträgen auf Reddit und Discord, und dort liest es niemand auf Deutsch. Dafür gibt es `I18nService.tIn(lang, key)` neben `t()`; wer im Zeichner `t()` benutzt, macht das Bild wieder zweisprachig. Die Bedienelemente um das Bild herum bleiben dagegen in der App-Sprache — die sieht nur, wer die App benutzt. **Der QR-Code unten rechts** führt auf `?deck=<id>` (siehe Navigation ganz oben) und damit in die öffentliche Deck-Ansicht - wer scannt, ist im Zweifel nicht eingeloggt. Er sitzt auf weißem Grund, weil Kameras einen QR-Code ohne helle Umrandung oft nicht erkennen, und **private Decks bekommen keinen** (`istPrivat`): Der Link liefe für jeden anderen ins Leere, und ein toter QR-Code auf einem Bild zum Teilen ist schlimmer als keiner. Die Adresse kommt aus `window.location.origin` - ein Steckbrief, der auf einer Cloudflare-Preview erzeugt wird, trägt deshalb einen QR auf diese Preview. Im öffentlichen Stöbern fehlt das Bracket-Abzeichen, weil `PublicDeckService` die Bracket-Spalten gar nicht lädt. Fehlt die Migration noch, verschwinden still nur die zwei Textfelder (`42703`/`PGRST204`), der Reiter bleibt.
- **Bilder im Primer, zwei Herkünfte und nur diese zwei:** Kartenbilder von Scryfall (Werkzeugkasten: Name eintippen, „Als Kartenbild") und eigene Uploads im Bucket `primer-images` (`sql/primer-bilder-bucket-2026-09-22.sql`, öffentlich lesbar, Schreiben nur in den Ordner der eigenen Benutzer-ID). Alles andere fällt in `BILD_QUELLEN` weg — auch ein anderer Bucket desselben Supabase-Projekts. Beide Herkünfte stehen bereits in der CSP (`public/_headers`), ein Bild von woanders würde der Browser ohnehin blocken. Eigener Bucket statt des vorhandenen `deck-art`: Dort liegen Kartenbilder, die ein Deck *statt* der Scryfall-Edition anzeigt; wer später aufräumen will, müsste die beiden sonst am Dateinamen auseinanderhalten. **Fehlt der Bucket, verschwindet der Knopf für eigene Bilder nach dem ersten Versuch** (`eigeneBilderVerfuegbar`) — Kartenbilder brauchen keinen Bucket und bleiben. Bilder sind `inline-block`, nicht `block`: Ein Bild darf mitten in einem Satz stehen; auf einer eigenen Zeile landet es weiterhin, wenn es allein in einem Absatz steht — das entscheidet der Text drumherum und nicht die Stilregel. **Kartenbilder sind mit 130 px halb so breit wie eigene Bilder** (erkannt am Scryfall-Präfix, also derselben Adresse, über die auch `BILD_QUELLEN` entscheidet): Eine Karte in voller Breite reißt den Satz auseinander, in dem sie steht — den Regeltext liest man ohnehin in der Vorschau, einen Klick entfernt. **Unterhalb von `--bp-sm` (Handy hochkant) werden Kartenbilder beim ANZEIGEN zum anklickbaren Kartennamen** (`primerKartenbilderAlsNamen()`, Breite über `matchMedia` wie in `game-session.service.ts`): Auch ein halbbreites Kartenbild lässt auf einem Handy kaum Text daneben, und mehrere hintereinander machen aus dem Primer eine Bildergalerie. Möglich ist das nur, weil der Kartenname beim Einfügen im `alt` landet — **eigene Uploads bleiben deshalb Bilder**, sie haben keinen Namen und sind ja gerade als Bild gemeint. Gespeichert bleibt in beiden Fällen das Bild.
- **Deck-Kommentare hängen am Deck, nicht an der Ansicht:** `deck-comment.service.ts` + `deck-comments/` werden an **beiden** Stellen eingehängt, an denen man ein fremdes Deck ansieht — `deck-detail-view` (Decks der eigenen Gruppe) und `public-deck-browser` (öffentliches Stöbern). Die beiden teilen sonst keinen Zustand, deshalb merkt sich der Service, zu **welchem** Deck die geladene Liste gehört (`loadedDeckId`), sonst überschreibt bei schnellem Deck-Wechsel eine überholende Antwort die Anzeige. Eine Antwort-Ebene, per Trigger serverseitig erzwungen (`sql/deck-kommentare-2026-09-22.sql`) — der Client bietet unter einer Antwort schlicht kein weiteres Antwortfeld an, aber das ist nur Oberfläche. **Gelesen wird über die Funktion `deck_comments_for_deck()`, nicht über die Tabelle:** Anzeigename und Avatar kommen beim **Lesen** aus `profiles` (ein beim Schreiben eingefrorener Name wäre nach der nächsten Umbenennung dauerhaft falsch — anders als bei `public.feedback`), und nur eine `SECURITY DEFINER`-Funktion liefert die auch nicht eingeloggten Besuchern, ohne dass `profiles` selbst für `anon` offen sein muss. Sichtbarkeit folgt exakt `decks.is_private`, Schreiben nur eingeloggt, Löschen darf Verfasser, Deck-Besitzer und Developer; **`update` hat bewusst gar keine Policy** — ein nachträglich umgeschriebener Kommentar unter fremdem Deck ist genau das, was man hier nicht will. Fehlt die Migration noch, schaltet sich der ganze Abschnitt beim ersten `PGRST202`/`42883` still ab, statt unter jedem Deck eine Fehlermeldung zu zeigen.
- **Bannliste je Format: aus Scryfall, nicht von Hand:** `format_banlist` (`sql/format-bannliste-2026-09-23.sql`) hält je App-Format (der Wert aus `decks.format`, **nicht** Scryfalls Schlüssel — App-„Brawl" ist Scryfalls `standardbrawl`, App-„Historic Brawl" ist `brawl`) die gebannten Karten, für Vintage zusätzlich `restricted`. Gefüllt von Teil 3 des Scryfall-Nachtlaufs (`BANN_FORMATE` in `scripts/sync-scryfall-bulk.js`, 14 kleine `banned:`-Suchen, **unabhängig** von der Bulk-Datei, braucht also kein `--force`). Ein neues Format in `DECK_FORMATS` gehört auch dorthin, sonst bleibt seine Liste still leer. `banlist.service.ts` prüft auf zwei Wegen: Listen (`deck-list` mit rotem Ausrufezeichen + Zeile mit den Karten, Kacheln im öffentlichen Stöbern `public-deck-browser` mit rotem Rahmen + Ausrufezeichen) seitenweise über die SQL-Funktion `deck_banned_cards(ids[])` — **ohne** SECURITY DEFINER, es gelten die RLS-Regeln von `decks`/`deck_cards` —, die beiden Deck-Ansichten (`deck-detail-view` und die geöffnete Ansicht im Stöbern, roter Hinweis oben + rot umrandete Karten) im Browser gegen die geladene Kartenliste, damit die eigene auch beim Bearbeiten vor dem Speichern stimmt. Die Regel steht deshalb zweimal (Maybeboard und Marken zählen nicht, `restricted` erst ab zwei Exemplaren) — wer sie ändert, ändert beide. Fehlt die Migration, verschwindet die Prüfung still. **Dazu die Bauregeln** (`deck-regeln.ts`): Kartenzahl je Format (Commander/PDH/Historic Brawl genau 100, Brawl genau 60, sonst mindestens 60) und Kopien (Singleton bzw. 4×, ausgenommen Standardländer, „any number of cards named" und „up to seven/nine"). **Die Grenzen stehen nur in `FORMAT_REGELN`** — die SQL-Funktion `deck_rule_facts(ids[])` (`sql/deck-regeln-2026-09-23.sql`) liefert für die Listen bloß Fakten (Kartenzahl, mehrfach vorhandene Karten samt Ausnahme), geurteilt wird im Client. Die Ausnahme-Erkennung steht dagegen zweimal (`eigeneKopienGrenze()` und SQL). `BanlistService.pruefe()` fasst für die Deck-Ansichten alles zusammen (rot markierte Karten + Hinweiszeilen), `problemsFor()` für die Listen.
- **Artwork-Sprache (Kartenbilder) ist NICHT die Oberflächensprache:** `art-languages.ts` (Codeliste) und `art-language.service.ts` halten, in welcher Sprache die **Kartenbilder** gezeigt werden — Scryfall-Codes (`en`, `de`, `fr`, …), Standard `en`, einstellbar im Profil, gespeichert wie die Oberflächensprache doppelt (localStorage + `profiles.art_language`, `sql/artwork-sprache-2026-09-22.sql`; fehlt die Migration, schaltet sich die Spalte beim ersten `42703` ab und die Einstellung gilt nur auf dem Gerät). Den Tausch macht `ScryfallService.inKartensprache()` an den vier Sammelstellen (`searchCards`, `findCard`, `findCardsBulk`, `fetchCommanderList`) plus `CardDataService.findCardsBulk()`. **Getauscht werden ausschließlich `imageUrl`/`backImageUrl` und der reine Anzeigewert `printedName`** — Name, Typzeile, Regeltext und Farbidentität bleiben englisch, weil die App genau diese Felder auswertet (Typzeilen-Prüfungen, Kartenname als Schlüssel in Decklisten und Statistik); eine übersetzte Typzeile wirft keinen Fehler, sondern erzeugt stillschweigend halbe Deck-Analysen. Nachgeschlagen wird gebündelt über `lang:xx (!"A" or !"B" …)` (25 Namen je Anfrage — ab 40 Gliedern lehnt Scryfall mit 400 ab, nacheinander statt parallel wegen 429; Sitzungs-Cache inklusive „gibt es in dieser Sprache nicht" und „Scryfall kennt zu diesem Druck gar kein Bild") — der sonst übliche Collection-Endpoint kennt keinen Sprachparameter. Auf Englisch kostet das Ganze **keine** zusätzliche Anfrage, jede andere Sprache umgeht den nächtlichen `scryfall_cards`-Cache (der ist englisch) und lädt entsprechend langsamer.
- **PDF-Druck: Artwork je Exemplar, eine Spalte an `decks`:** Im PDF-Dialog (`deck-pdf-dialog/`, `deck-pdf.service.ts`) bekommt jede mehrfach vorhandene, einseitige Karte (Standardländer, Nazgûl, Seven Dwarves) ein Bild-Symbol; darunter lässt sich je Exemplar ein Druck aus `ScryfallService.getPrintings()` wählen oder per Knopf „Verschiedene Artworks verteilen“ automatisch zuteilen. Gespeichert in `decks.copy_artworks` (jsonb, `{kartenname_klein: [url|null, …]}`, `sql/deck-exemplar-artworks-2026-09-23.sql`) — **nicht** an `deck_cards`, weil die Kartenliste beim Speichern gelöscht und neu geschrieben wird. Wie beim Primer keine neue Policy; geschrieben wird nur, wenn `canEditViewingDeck()`, sonst gilt die Auswahl nur für den einen Druck. Die Auswahl betrifft **nur** das PDF, nicht die Deck-Anzeige. „Nur 1 Bild“ druckt jedes verschiedene Artwork einmal. Doppelseitige Karten bleiben außen vor (zum gewählten Vorderseiten-Druck fehlte die Rückseite). Fehlt die Migration, bleibt die Auswahl beim ersten `42703`/`PGRST204` still ungespeichert.
- **Cloudflare Functions:** `functions/api/proxy-image.ts` (CORS-Proxy für Scryfall-Bilder im PDF-Export), `functions/api/estimate-bracket.ts`
- **CSP und Cache-Header:** `public/_headers` — wer eine neue externe API anbindet, muss sie hier freischalten, sonst blockt der Browser sie stillschweigend.
- **Supabase-Migrationen:** `sql/` (datierte Skripte). **Diese Dateien laufen nicht automatisch** — sie müssen von Hand im Supabase-SQL-Editor ausgeführt werden, ein Merge allein ändert an der Datenbank nichts. Wer einem Fehler nachgeht, der nach „die App speichert nicht“ aussieht, prüft deshalb zuerst die Browser-Konsole: Rechte-Fehler kommen als HTTP 500 mit Postgres-Codes wie `42P17` an, nicht als Code-Fehler (siehe `sql/fix-tournament-rls-recursion-2026-09-03.sql`). **Wenn die Datenbank am 500-MB-Limit des Free-Plans steht:** `sql/datenbankgroesse-pruefen-2026-09-20.sql` zeigt Tabellen- und Indexgrößen, Aufblähung und Zuwachs je Tabelle; **die erste Messung (20.09.2026, 398 MB) steht im Kopf der Datei.** Kurz: Die **echten App-Daten sind 7 MB** (2 %) — der Rest ist Spellbook-Cache (193 MB), Archidekt-Deckvorrat (110 MB) und Scryfall-Cache (48 MB). Zwei naheliegende Verdächtige sind damit **widerlegt**: Die nächtlichen Abgleiche schreiben zwar jede Nacht alles neu (`spellbook_combos`: 1.377.393 Updates auf 105.025 Zeilen), aber `autovacuum` kommt hinterher — 5,2 % tote Zeilen, nicht 50 %. Und die Indizes sind nicht aufgebläht, sondern teuer entworfen: `spellbook_combo_cards_pkey` ist mit 33 MB fast so groß wie seine Tabelle, weil der Schlüssel `(combo_id text, name_normalized text)` bei 383.229 Zeilen rechnerisch genau so viel kostet. **Das war derselbe Fehler wie bei `archidekt_deck_pool_cards`** (dort 72 MB nur für den Primärschlüssel) — **behoben am 20.09.2026** durch `sql/spellbook-combo-card-arrays-2026-09-20.sql` (ein Array je Combo statt 3,6 Zeilen). Sofort verfügbar ohne Umbau: `deck_sim_results` hält die Fassungen 3, 5 und 7 (Fassung 8 hat nie einen Lauf gesehen), 3 und 5 sind ~17 MB. **Beim Löschen niemals „alles außer der aktuellen Fassung“ rechnen** — das hätte hier die ganze Tabelle geleert, samt der Fassung 7, auf der die Trennschärfe-Messung beruht.
- **Nächtliche Abgleiche (GitHub Actions):** `scripts/sync-scryfall-bulk.js` (Workflow `scryfall-sync.yml`, füllt `scryfall_cards`/`scryfall_card_effects`, Tabellen in `sql/scryfall-cache-2026-09-06.sql`) und `scripts/sync-spellbook-bracket.js` (Workflow `spellbook-sync.yml`, füllt `spellbook_card_flags`/`spellbook_two_card_combos`, Tabellen in `sql/spellbook-cache-2026-09-06.sql`, sowie `spellbook_combos`/`spellbook_combo_cardlists` samt der Suchfunktion des Combo-Finders, Tabellen in `sql/spellbook-combos-2026-09-07.sql` und — **maßgeblich für die Kartenliste** — `sql/spellbook-combo-card-arrays-2026-09-20.sql`; die Indizes auf `synced_at` ebendort und in `sql/spellbook-sync-cleanup-2026-09-16.sql` — ohne sie liest Postgres beim nächtlichen Aufräumen die ganze Tabelle und der Lauf stirbt an einer Zeitüberschreitung). **Die Kartenliste je Combo ist ein Zahlen-Array, keine Zeile je Karte:** `spellbook_combo_cardlists.card_ids` verweist auf `spellbook_card_names.id`, gefunden wird über den GIN-Index (`&&`). Die frühere Form `spellbook_combo_cards` kostete 82 MB, davon 33 MB allein für den Primärschlüssel `(combo_id text, name_normalized text)` — dieselbe Falle wie bei `archidekt_deck_pool_cards`. Wer die Kartennamen braucht, `unnest`t `card_ids` und joint auf `spellbook_card_names`). Beide brauchen das Repo-Secret `SUPABASE_SERVICE_ROLE_KEY` und sind über „Actions“ von Hand auslösbar. Sie holen öffentliche Kartendaten einmal pro Nacht von einem Server, statt sie bei jedem Deck-Öffnen aus jedem Browser nachzufragen.
- **Deck-Import von Archidekt (GitHub Action, NICHT nächtlich):** `scripts/import-archidekt-decks.js` (Workflow `archidekt-import.yml`, füllt `archidekt_deck_pool`/`archidekt_deck_pool_cards`, Tabellen in `sql/archidekt-deck-pool-2026-09-15.sql`). Holt fremde Commander-Decks samt der Bracket-Stufe, die ihr Ersteller selbst angegeben hat, als Referenzvorrat für die Bracket-Auswertung. Nur von Hand auslösbar („Actions“ → Bracket und Anzahl eintragen), braucht dasselbe Secret `SUPABASE_SERVICE_ROLE_KEY`; `--dry-run` läuft ohne Secret und ohne `node_modules`. Der Vorrat ist bewusst von `decks` getrennt und nur für Developer sichtbar — diese Decks gehören niemandem in der App und dürfen nirgends als Spieler-Decks erscheinen. Aus dem Browser wäre der Import unmöglich: Archidekt antwortet mit einem festen fremden `access-control-allow-origin`, ein `fetch` aus der App scheitert immer an CORS. Die Developer-Ansicht (`archidekt-pool-browser/`) sucht und filtert **server-seitig** — sie braucht zusätzlich `sql/archidekt-pool-search-2026-09-15.sql` (Suchspalte + Trigramm-Index); ohne diese Migration meldet sie nur noch einen Ladefehler. Die Kartenlisten liegen als Zahlen-Arrays in `archidekt_deck_pool_cardlists` mit der Namenstabelle `archidekt_pool_card_names` (`sql/archidekt-pool-card-arrays-2026-09-15.sql`) — **eine Zeile je Deck, nicht je Karte**: Die frühere Form hat bei 10.000 Decks 160 MB statt 9 MB belegt und den 500-MB-Free-Plan fast gesprengt. Wer die Kartenliste braucht, `unnest`t die beiden parallelen Arrays und joint über `name_normalized` auf `scryfall_cards`.
- **Goldfish-Simulation (GitHub Action, NICHT nächtlich):** `scripts/simulate-deck-pool.js` (Workflow `deck-sim.yml`, füllt `deck_sim_results`, Tabelle und Auswertungs-Ansichten in `sql/deck-sim-results-2026-09-16.sql`, Liste der spielbeendenden Combos in `sql/spellbook-winning-combos-matview-2026-09-16.sql`). Spielt jedes Deck aus dem Archidekt-Vorrat ein paar hundert Mal ohne Gegner aus und misst, in welchem Zug es gewinnen könnte — plus gezählte Kennzahlen (Rampe, Kartenziehen, Interaktion, Tutoren, Manakosten). Zweck: die offiziellen Bracket-Kriterien zählen Karten und messen kein Tempo; dieser Lauf liefert die Gegenprobe an Decks, deren Stufe jemand anders festgelegt hat. Die **Spielweise** ist eine feste Rangfolge (`rangFuer` in `goldfish-sim.ts`): erst Mana und Rampe, dann Synergie-Bausteine, dann Ziehen und Suchen, dann bleibende Combo-Teile, dann der Rest. Ein Combo-Teil, das nach dem Wirken im Friedhof liegt, wird **nie** gewirkt — geschützt sind dabei die Karten **aller** gewinnenden Combos des Decks (`SimDeck.comboTeile`), nicht nur der zehn, die der Simulator aktiv verfolgt. Seit Fassung 6 (`sql/deck-sim-v6-2026-09-16.sql`) kommen vier Messgrößen dazu: aufaddierter Schaden bis Zug 10, Streuung (p75−p25 der Siegzüge), Leerlaufzüge und Mulligans. **Was davon taugt, ist gemessen und steht in `sql/deck-sim-feature-strength-2026-09-16.sql`** — kurz: nichts davon. Von allem, was die Simulation ausgibt, trennt genau eine Zahl die Bracket-Stufen brauchbar, nämlich das **verfügbare Mana in Zug 3** (AUC 0,660). Jede Zahl über das Gewinnen liegt im Rauschen, und der Schaden bis Zug 10 zeigt sogar in die falsche Richtung. Die fertige Gegenüberstellung steht in der Ansicht `deck_sim_by_bracket` (nur noch Zeilen mit `karten_erkannt >= 0,6` — die Spalte gab es vorher schon und hat nie gefiltert) — eine Spalte, deren Werte von Stufe 2 zu Stufe 4 nicht monoton laufen, trennt die Stufen nicht. Seit Fassung 8 (`sql/sieg-definition-breit-2026-09-17.sql`) steht neben `gewinn_combos` (eng) die Spalte `sieg_combos` (weit) — beide nebeneinander, damit sich messen lässt, welche die Stufen besser trennt, statt die Definition auf Verdacht auszutauschen. Ebenfalls seit Fassung 8 bedeutet `karten_erkannt` **Erkennung, nicht Spielbarkeit**: Eine Tabelle in `sim-card-profile.ts` markiert Karten, die der Simulator versteht, aber im Goldfish nicht ausspielen kann (Konter, Bounce, Handstörung, Umlenken — alles, was einen Gegner braucht), mit `reaktiv`; `rangFuer` sortiert die ganz nach hinten, die Erkennungsquote zählt sie aber mit. Quoten aus Läufen vor Fassung 8 sind deshalb nicht vergleichbar. **Die Spiellogik steht NICHT im Skript**, sondern in `src/app/goldfish-sim.ts` und `src/app/sim-card-profile.ts`, weil dieselbe Rechnung später im Browser für ein einzelnes Deck laufen soll; das Skript bündelt die beiden Module zur Laufzeit mit esbuild. Braucht `SUPABASE_SERVICE_ROLE_KEY`, `--dry-run` rechnet ohne zu schreiben. Die gewinnenden Combos liest er aus einer **materialisierten** Ansicht — als gewöhnliche Ansicht rechnete dieselbe Abfrage die Verknüpfung über 369.706 Kartenzeilen bei jedem der zwanzig Pakete neu und lief irgendwann in die Zeitüberschreitung. Aufgefrischt wird sie am Ende des Spellbook-Abgleichs; von Hand geht `select public.refresh_spellbook_winning_combos();`. Der Simulator braucht `power`/`toughness` in `scryfall_cards` (`sql/scryfall-sim-fields-2026-09-16.sql`) — nach dieser Migration den Scryfall-Abgleich **mit `--force`** anstoßen, sonst lädt er die Bulk-Datei gar nicht erst und die neuen Spalten bleiben leer.
- **Legalitätsprüfung des Deckvorrats (GitHub Action, NICHT nächtlich):** `scripts/check-deck-legality.js` (Workflow `deck-legality.yml`, füllt `archidekt_deck_pool.legal`/`illegale_karten`, Spalten in `sql/deck-pool-legality-2026-09-16.sql`, Kartenlegalität in `sql/scryfall-commander-legality-2026-09-16.sql`). Markiert Decks, deren Karten im Commander nicht spielbar sind — **markiert, löscht nicht**: Der Import zieht Decks nach ihrer Archidekt-ID, ein gelöschtes Deck wäre beim nächsten Import wieder da. Drei Zustände, und der dritte zählt: `true` geprüft und legal, `false` geprüft und durchgefallen, **`null` noch nicht geprüft**. Die Simulation schließt nur `false` aus, nie `null` — ungeprüft heißt nicht illegal. Setzt voraus, dass der Scryfall-Abgleich **mit `--force`** gelaufen ist, sonst steht `commander_legal` überall auf null; das Skript bricht in dem Fall von selbst ab, statt jedes Deck als legal zu markieren. Die Developer-Ansicht zeigt je Bracket, wie viele Decks legal sind. **Der Schlüssel `front_name_normalized` ist NICHT eindeutig** — 45 Namen haben mehrere Einträge, bei 21 davon ist einer spielbar und ein anderer nicht („Savage Lands" als Land und als Eintrag mit der Typzeile „Card", „Smelt" als Instant und als Playtest-Karte). Wer über diesen Schlüssel nachschlägt, muss die spielbare Fassung bevorzugen; die erste Fassung der Prüfung tat das nicht und sortierte 875 Decks allein wegen Savage Lands aus.
- **Urteil F der Bracket-Einstufung (empirisch):** `src/app/bracket.ts` hebt ein Deck auf mindestens Bracket 4, wenn es eine spielbeendende Combo **und** mindestens zwei Tutoren enthält. Das ist das einzige Kriterium dort, das **nicht** aus dem Regelwerk stammt, sondern aus dem Stapellauf über 48.638 fremde Decks: 93 % der Decks mit dieser Verbindung nennen sich selbst Bracket 4 oder 5, von Stufe 2 zu Stufe 4 ist es Faktor 30. Die Zahl der Combos liefert die Datenbankfunktion `winning_combos_in_deck` (`sql/winning-combos-in-deck-2026-09-16.sql`, setzt die materialisierte Ansicht voraus), aufgerufen von `CardDataService.winningCombosIn()`. Ohne die Funktion kommt 0 zurück und Urteil F hebt nicht an — die Einstufung läuft dann wie vorher.
- **Zwei Sieg-Definitionen, jede steht genau einmal:** seit `sql/sieg-definition-breit-2026-09-17.sql` gibt es **eng** (`SOFORT_SIEG_MUSTER` / `spellbook_sofort_sieg_muster()`) und **weit** (`SIEG_MUSTER` / `spellbook_winning_combo_muster()`), dazu die Ausschlussliste `SIEG_AUSNAHME` / `spellbook_sieg_ausnahme()` — alle drei in `src/app/goldfish-sim.ts` und als Postgres-Funktion. **Eng** heißt „beendet das Spiel unmittelbar“ und ist die Grundlage von Urteil F; die Liste bleibt unverändert, weil die Schwellen daran geeicht wurden, und `winning_combos_in_deck()` filtert weiter über die Spalte `beendet_sofort`. **Weit** heißt „ist der Endpunkt eines Decks“ und ist die Grundlage der Simulation. Der Anlass war messbar: ein Urza-cEDH-Deck enthält 22 vollständige Combos, von denen nach der engen Definition **keine einzige** ein Sieg ist (die Ergebnisse heißen „Infinite storm count“, „Cast all spells in your library“) — der Simulator meldete 0 Siege in 1000 Spielen, mit der weiten Definition 59,7 %. Geprüft ist das gegen die vollständige Merkmalsliste von Spellbook (1.320 Ergebnisse): 43 zählen weit, 28 eng, neun fallen durch die Ausnahmen („Infinite damage to all creatures“ ist ein Boardwipe, „Infinite self-mill“ ohne Laborschwester das Gegenteil eines Sieges). Die Muster standen früher doppelt (TypeScript und SQL) und liefen auseinander; `scripts/simulate-deck-pool.js` vergleicht deshalb **alle drei** vor jedem Lauf und bricht bei Abweichung ab. Wer einen Ausdruck ändert: **kein `\b`** — in Postgres ist das ein Rückschritt-Zeichen, keine Wortgrenze.
- **Trennschärfe der Merkmale (nur SQL, kein Skript):** `sql/deck-sim-feature-strength-2026-09-16.sql` legt die Funktion `deck_sim_auc()` und die Ansicht `deck_sim_feature_strength` an. Sie beantwortet je Merkmal aus `deck_sim_results`, wie gut es zwei Bracket-Stufen unterscheidet (die Fassung steht seit `sql/deck-sim-v6-2026-09-16.sql` **nicht** mehr als Literal in der Signatur, sondern ist die neueste vorhandene — sonst wäre die Ansicht beim ersten Simulator-Update stillschweigend blind geworden) — als AUC über Rängen (Mann-Whitney), also ohne vorher gesetzte Schwelle. **0,5 heißt „trennt gar nicht"**, der Abstand zu 0,5 ist die Stärke, die Richtung nur ihr Vorzeichen. Wozu: Ein Mittelwert, der von Stufe zu Stufe steigt, sieht nach Erkenntnis aus und kann trotzdem wertlos sein, wenn die Verteilungen überlappen. **Die erste vollständige Messung (17.09.2026, 48.638 Decks) steht im Kopf der Datei** und ist die wichtigste Seite dieses ganzen Projekts: Game Changer (0,898) und Tutoren (0,771) trennen mit Abstand am besten — beide stehen längst in `bracket.ts`. Die Interaktion trennt gar nicht (0,509). Und die Rauschgrenze heißt `karten_erkannt` (0,096): Die Spalte, die misst, wie viel der Simulator von einem Deck überhaupt versteht, trennt die Stufen besser als fast jede Zahl, die er ausrechnet. Die Ansicht braucht fünf bis zehn Sekunden und gehört in den SQL-Editor, nicht in die App.
- **Generiert, niemals von Hand anfassen:** `src/app/version.ts` (erzeugt von `scripts/generate-version.js` bei jedem `start`/`build`, gitignored)

---

## Arbeitsregeln

Diese Regeln sparen Kontext und damit Zeit und Kosten. Sie sind nicht optional.

### UI-Texte: erst die richtige Datei bestimmen, dann greppen

Alle sichtbaren Texte liegen in `src/app/i18n/` — **je Bereich eine Datei, beide Sprachen darin nebeneinander.** `i18n.service.ts` selbst ist nur noch die Klasse plus das Zusammensetzen der Module (~110 Zeilen) und enthält **keine** Texte mehr.

```ts
// src/app/i18n/match.ts
export const match = {
  de: { 'match.newMatch': 'Neues Match', … },
  en: { 'match.newMatch': 'New match', … },
};
```

Das **Key-Präfix sagt, in welcher Datei der Key steht** (Schema `bereich.beschreibung`):

| Präfix                                                                                                                                                     | Datei           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| `match`, `game`, `placement`                                                                                                                               | `match.ts`      |
| `stats`                                                                                                                                                    | `stats.ts`      |
| `group`, `permission`                                                                                                                                      | `group.ts`      |
| `profile`, `inbox`                                                                                                                                         | `profile.ts`    |
| `deck`, `deckComment`, `deckPrimer`, `deckSocial`, `deckSteckbrief`, `deckViewer`, `importDialog`, `pdfDialog`                                                                                           | `deck.ts`       |
| `deckView`                                                                                                                                                 | `deck-view.ts`  |
| `tournament`, `tournamentHistory`                                                                                                                          | `tournament.ts` |
| `ingame`, `goldfish`                                                                                                                                       | `ingame.ts`     |
| `archetypeFilter`, `effectFilter`, `keywordFilter`, `commanderRec`, `precons`, `publicDecks`, `publicSearch`, `search`, `colorFilter`, `colorCombo`, `pip` | `search.ts`     |
| `login`, `loginRequired`, `resetPassword`                                                                                                                  | `auth.ts`       |
| `feedback`                                                                                                                                                 | `feedback.ts`   |
| `tutorial`                                                                                                                                                 | `tutorial.ts`   |
| `dialog`, `common`, `nav`, `header`, `sort`, `legal`, `cardImage`, `partnerCardImage`                                                                      | `common.ts`     |

Vorgehen bei einer Textänderung:

1. Ist der Bereich schon klar, direkt in der passenden Datei greppen. Sonst über den sichtbaren Text suchen: `grep -rn "Neues Match" src/app/i18n/`
2. Nur die gefundenen Zeilen editieren — die Module sind klein (größte: `tutorial.ts` 409, `deck-view.ts` 404 Zeilen), ein ganzes Modul zu lesen ist vertretbar.
3. **Immer beide Sprachen anpassen** — `de` _und_ `en` stehen in derselben Datei direkt untereinander. Ein Key, den es nur in einer Sprache gibt, ist ein Bug; `src/app/i18n/i18n-keys.spec.ts` prüft das.
4. Ein **neuer** Key gehört in das Modul seines Präfixes. Ein neues Präfix muss zusätzlich in die Tabelle in `i18n-keys.spec.ts` **und** in die Präfix-Tabelle oben eingetragen werden, sonst schlägt der Test fehl.

### Die Karte mitpflegen

Die Architektur-Karte oben ist nur so lange etwas wert, wie sie stimmt. **Eine falsche Karte ist schlimmer als gar keine** — sie schickt die nächste Session gezielt an den falschen Ort. Das ist schon passiert: Die Karte entstand in PR #164 und war vier Commits später falsch, weil die i18n-Aufteilung sie nicht mitzog; die Korrektur brauchte einen eigenen PR (#167).

**Wer eine Datei anlegt, löscht oder umbenennt, die in die Karte gehört, aktualisiert die Karte im selben PR.** Das betrifft:

| Was du änderst                        | Was in der Karte nachgezogen wird                    |
| ------------------------------------- | ---------------------------------------------------- |
| Komponente unter `src/app/`           | Komponenten-Tabelle                                  |
| Service/Helper flach in `src/app/`    | Services-Tabelle                                     |
| Baustein in `src/app/ui/`             | Abschnitt „Wiederverwendbare UI-Bausteine"           |
| Modul in `src/app/i18n/`              | i18n-Abschnitt inkl. Modulanzahl und Präfix-Tabelle  |
| Eine Datei wächst über 1000 Zeilen    | Tabelle „Große Dateien"                              |

Ein PR, der die Struktur ändert und die Karte stehen lässt, ist unvollständig — genauso wie ein i18n-Key, den es nur auf Deutsch gibt.

`npm run check:map` sagt dir, was fehlt. Der Check läuft in einer Sekunde und prüft in **beide** Richtungen: ob alles Vorhandene in der Karte steht, und ob alles in der Karte Genannte noch existiert (fängt Umbenennungen ab, die sonst still ins Leere zeigen). Er ist bewusst **nicht** an `npm run build` gekoppelt — Cloudflare Pages baut mit genau diesem Kommando, eine veraltete Karte darf das Deployment nicht blockieren.

### Große Dateien: erst suchen, dann gezielt lesen

Bei diesen Dateien grundsätzlich `grep`/`Glob` vor `Read`; wenn doch gelesen werden muss, mit `offset`/`limit` nur den relevanten Abschnitt:

| Zeilen | Datei                                            |
| ------ | ------------------------------------------------ |
| 3475   | `src/app/deck-viewer.service.ts`                 |
| 1770   | `src/app/tournament.service.ts`                  |
| 2804   | `src/app/deck.service.ts`                        |
| 1492   | `src/app/mtg.service.ts`                         |
| 1233   | `src/app/scryfall.service.ts`                    |
| 1386   | `src/app/stats-tab/stats-tab.ts`                 |
| 1111   | `src/app/game-session.service.ts`                |
| 1154   | `src/app/profile-tab/profile-tab.ts`             |
| 1727   | `src/app/deck-detail-view/deck-detail-view.html` |
| 1075   | `src/app/sim-card-profile.ts`                    |
| 1015   | `src/app/goldfish-sim.ts`                        |

`src/app/excel-import.service.ts` wird von grep als binär erkannt (eingebettete Daten) — nicht am Stück lesen.

### Reihenfolge beim Suchen

1. Diese Karte und die Vokabular-Tabelle in `PROMPTING.md`
2. `grep` mit einem konkreten Begriff aus der Aufgabe
3. Erst dann breite Erkundung

**Keine Sub-Agenten für Aufgaben, deren Bereich schon feststeht.** Ein Sub-Agent, der eine bekannte Datei nachschlägt, kostet ein Vielfaches eines gezielten `grep`.

### Umfang

Nur das ändern, wonach gefragt wurde. Keine ungefragten Refactorings, keine „Verbesserungen" nebenbei — der User testet jeden PR einzeln über die Preview-URL, und ein PR mit drei unabhängigen Änderungen ist nicht prüfbar.

---

## Verifikation

| Befehl                         | Was es prüft                                                                 |
| ------------------------------ | ---------------------------------------------------------------------------- |
| `npm run typecheck`            | Dev-Build inkl. `strictTemplates` — die schnellste echte Prüfung, erste Wahl |
| `npm run build`                | Prod-Build inkl. Budgets — vor jedem PR                                      |
| `npm run check:map`            | Ob die Architektur-Karte oben noch zum Repo passt — nach Strukturänderungen |
| `npx prettier --check <datei>` | Prettier auf den **selbst geänderten** Dateien                               |
| `npm run format:check`         | Prettier projektweit — schlägt derzeit an ~104 Altdateien an, siehe unten    |
| `npm test`                     | Vitest                                                                       |

Wichtig zur Einordnung:

- `npm run format:check` meldet aktuell **~104 vorbestehende** Dateien: Prettier ist konfiguriert, wurde aber nie projektweit ausgeführt. Ein roter `format:check` ist deshalb **kein** Hinweis darauf, dass die eigene Änderung falsch formatiert ist. Prüfe gezielt die eigenen Dateien (`npx prettier --check <datei>`) und formatiere auch nur diese. **Nicht** `npm run format` über das ganze Projekt laufen lassen — das erzeugt einen themenfremden Riesen-Diff, den der User nicht prüfen kann.
- Es gibt **kein Lint** und **keine Build-CI auf GitHub**. Die drei Workflows sind alle nächtliche Hintergrundläufe und sagen über einen PR nichts aus: das Supabase-Backup sowie der Scryfall- und der Commander-Spellbook-Abgleich (siehe „Weitere Orte“). Ein grüner PR bedeutet also nicht, dass gebaut wurde — deshalb lokal bauen, bevor gepusht wird.
- Es gibt **13 Spec-Dateien** (`scryfall.service`, `public-deck.service`, `deck.service`, `color-filter-match`, `color-combo-names`, `app-recovery`, `bracket`, `combo-finder`, `goldfish-sim`, `sim-card-profile`, `primer-html`, `ui/radar-chart/radar-geometry`, `i18n/i18n-keys`). Die Tests sind **kein Sicherheitsnetz** — grüne Tests sagen fast nichts. Am dichtesten getestet ist der Simulator; dort ist ein roter Test ein echtes Signal.
- Der echte Test ist die **Cloudflare-Pages-Preview des PRs** auf dem iPhone.

---

## Konventionen

- **Commit-Messages auf Deutsch**, knapp und beschreibend, im Stil der bestehenden History — keine Conventional-Commits-Präfixe. Beispiele: `Farbfilter mit Mehrfachauswahl, Farbnamen aus den Suchfiltern raus`, `Profil-Tab: Umschalter kompakt, keine leeren Riesenkaesten mehr`, `Footer ans Seitenende statt direkt unter den Inhalt`.
- Neue Komponenten bekommen **keine** Spec-Datei (`skipTests: true` in `angular.json`).
- Mobile-first: Layouts zuerst für iPhone/Safari denken, Desktop danach.
- Neue externe Domains gehören in die CSP in `public/_headers`.

---

## Test-Accounts: die App selbst anschauen und Änderungen validieren

Die App läuft lokal gegen die **Produktiv**-Supabase. Damit kann jede Session die laufende App
selbst bedienen — einloggen, durchklicken, Screenshots machen — statt Änderungen nur im Code zu prüfen.

**Es gibt keine festen Testkonten mehr.** Die früheren vier Konten der „Claude Testgruppe" wurden
am 23.09.2026 zusammen mit rund 30 liegengebliebenen Wegwerfkonten gelöscht
(`sql/konten-aufraeumen-2026-09-23.sql`). Die Registrierung ist offen und `mailer_autoconfirm` ist
aktiv, ein frisches Konto ist also in Sekunden einsatzbereit. Wer eines braucht:

- **E-Mail immer nach dem Muster `claude.<zweck>.<zeitstempel>@example.com`** — nur so erkennt
  das Aufräum-Skript das Konto später wieder. Ein Konto unter anderem Namen bleibt für immer liegen
  und landet in der Kontenliste des Users.
- **Nie in eine echte Gruppe eintreten** und keine Partien in einer echten Gruppe speichern — das
  Aufräum-Skript bricht dann ab, statt echte Partien mitzulöschen. Für Statistik-Ansichten eine
  eigene Gruppe anlegen und dort ein paar Matches spielen.
- **Ein Konto je Session reicht.** Nach dem Test nicht selbst löschen (die Session hat keinen
  Service-Key); der User lässt `sql/konten-aufraeumen-2026-09-23.sql` bei Gelegenheit erneut laufen.

### Erwartung an neue Sessions

Wer die UI ändert, prüft die Änderung **in der laufenden App** und belegt sie mit einem
Screenshot — nicht nur mit `npm run typecheck`. Das Projekt hat kein Lint, keine Build-CI und
praktisch keine Tests; der Screenshot ist der einzige echte Nachweis.

### So geht es

`npm start` (Dev-Server auf `localhost:4200`, spricht mit der Produktiv-Supabase), dann
Playwright 1.56 und Chromium — beide sind in der Umgebung vorinstalliert, **kein**
`playwright install`:

```js
const browser = await chromium.launch({
  executablePath: '/opt/pw-browsers/chromium',
  args: [
    '--no-sandbox',
    '--disable-quic',
    '--disable-features=PostQuantumKyber,TLS13KyberSupport,EncryptedClientHello,UseDnsHttpsSvcb',
    '--ssl-version-max=tls1.2',
  ],
});
const ctx = await browser.newContext({ viewport: { width: 390, height: 844 }, isMobile: true, locale: 'de-DE' });
```

Vier Stolpersteine, die sonst jede Session neu sucht:

1. **Keine `proxy:`-Option setzen.** Chromium übernimmt den Proxy aus der Umgebung, und
   `no_proxy` nimmt `localhost` bereits aus. Eine explizite Proxy-Option schickt auch die
   Anfragen an `localhost:4200` in den Proxy — die App lädt dann gar nicht.
2. **Die TLS-Flags oben sind Pflicht.** Ohne sie bricht der Tunnel zu Supabase mit
   `ERR_CONNECTION_RESET` ab; der Post-Quantum-ClientHello ist dem Proxy zu groß.
3. **Das Tutorial-Overlay erscheint pro Tab neu** und fängt alle Klicks ab. Nach jedem
   Tabwechsel „Überspringen" klicken, sonst laufen alle folgenden Klicks in den Timeout.
4. **Stabile Selektoren:** Tabs tragen `data-tutorial="nav-match|search|stats|group|profile"`,
   Dialoge liegen in `.options-menu-sheet`, Bestätigungen in `app-dialog` (Button `.primary`).

Ablauf für Matches (erzeugt Statistiken): Match-Tab → Spieler-Chips wählen → je Spieler
„📚 Deck wählen" → „▶ Spiel starten" → im Tracker ⋮ → „🏁 Spiel beenden" → Sieger 🏆 →
„Match speichern & beenden" → den `app-dialog` bestätigen.

---

## Weiterführend

`PROMPTING.md` im Projektwurzelverzeichnis erklärt dem User, wie er Aufgaben formuliert. Die dortige Vokabular-Tabelle („was der User sagt" → „welche Datei") ist auch für Claude nützlich.
